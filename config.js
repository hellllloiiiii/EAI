// Everday AI backend configuration.
// After creating your Supabase project, paste its Project URL and anon (public)
// key here (Supabase Dashboard -> Project Settings -> API). The anon key is
// safe to publish; database rules control what it can do. Never put the
// service_role key in this file.
// While these are empty, the site runs in offline mode: progress is saved on
// the device only and the tester code still works.
window.EAI_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: ""
};
